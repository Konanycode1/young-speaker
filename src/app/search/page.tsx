import { Search } from "lucide-react";
import { ArticleCard } from "@/components/article-card";
import { articles } from "@/lib/data";
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const {q=""}=await searchParams;const results=q?articles.filter(a=>`${a.title} ${a.excerpt} ${a.category} ${a.author}`.toLowerCase().includes(q.toLowerCase())):[];return <><section className="page-hero container"><span className="eyebrow">Recherche</span><h1>Qu’est-ce qui te parle ?</h1><form className="search-box"><Search/><input className="search-input" name="q" defaultValue={q} placeholder="Un sujet, un mot, un Speaker…" autoFocus/></form></section><section className="container section" style={{paddingTop:0}}>{q?<><div className="section-head"><h2>{results.length} résultat{results.length!==1?"s":""} pour « {q} »</h2></div>{results.length?<div className="article-grid">{results.map(a=><ArticleCard key={a.slug} article={a}/>)}</div>:<div className="empty-note">Aucune voix trouvée. Essaie un sujet plus général, comme « santé » ou « école ».</div>}</>:<div className="empty-note">Commence à écrire pour explorer les voix de la communauté.</div>}</section></>}
