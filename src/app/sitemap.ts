import type {MetadataRoute} from "next";import {articles} from "@/lib/data";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_APP_URL??"http://localhost:3000";return ["","/articles","/speakers","/themes","/challenges","/become-speaker","/safety","/terms","/privacy"].map(p=>({url:`${base}${p}`,lastModified:new Date()})).concat(articles.map(a=>({url:`${base}/articles/${a.slug}`,lastModified:new Date(a.date)})))}
